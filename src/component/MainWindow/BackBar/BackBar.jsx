import React from "react";
import cd from "./BackBar.module.css";

let BackBar = () => {
    return(
        <div className={cd.navBack}>
            Back
        </div>
    )
}

export default BackBar;